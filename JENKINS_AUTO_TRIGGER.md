# 🔄 Настройка автоматического запуска Jenkins при push в Git

## ❌ Проблема
Jenkins не запускает сборку автоматически после push в Git.

## ✅ Решение: Настроить автоматический триггер

Есть **2 способа** настроить автоматический запуск:

---

## 🎯 Способ 1: Poll SCM (РЕКОМЕНДУЕТСЯ для зачета)

**Poll SCM** - Jenkins периодически проверяет репозиторий на изменения.

### Настройка:

1. **Откройте Jenkins:**
   - http://localhost:8080 (или http://localhost:8081)

2. **Откройте ваш Pipeline:**
   - Найдите `CabinetBooking-CI-CD`
   - Нажмите на него

3. **Откройте настройки:**
   - Нажмите **"Configure"** (слева)

4. **Найдите раздел "Build Triggers":**
   - Прокрутите вниз до раздела **"Build Triggers"**

5. **Включите Poll SCM:**
   - ✅ Отметьте **"Poll SCM"**
   - В поле **"Schedule"** введите: `H/2 * * * *`
     - Это означает: проверять каждые 2 минуты
     - Или `H/5 * * * *` - каждые 5 минут

6. **Сохраните:**
   - Нажмите **"Save"**

### Как работает:

- Jenkins будет проверять репозиторий каждые 2 минуты
- Если обнаружены изменения - автоматически запустит сборку
- Не требует настройки webhook на GitHub

### Расписание (Cron синтаксис):

- `H/2 * * * *` - каждые 2 минуты
- `H/5 * * * *` - каждые 5 минут
- `H * * * *` - каждый час
- `H H * * *` - каждый день в случайное время

**Для зачета используйте:** `H/2 * * * *` (каждые 2 минуты)

---

## 🎯 Способ 2: GitHub Webhook (мгновенный запуск)

**Webhook** - GitHub отправляет запрос в Jenkins при push.

### Настройка в Jenkins:

1. **Откройте настройки Pipeline:**
   - Configure → Build Triggers
   - ✅ Отметьте **"GitHub hook trigger for GITScm polling"**
   - Save

2. **Установите плагин (если не установлен):**
   - Manage Jenkins → Manage Plugins
   - Вкладка "Available"
   - Найдите "GitHub plugin"
   - Установите и перезапустите Jenkins

### Настройка на GitHub:

1. **Откройте репозиторий:**
   - https://github.com/Psidjik/adminka1

2. **Перейдите в Settings:**
   - Settings → Webhooks → Add webhook

3. **Настройте webhook:**
   - **Payload URL:** `http://localhost:8080/github-webhook/`
     - ⚠️ Для localhost это не сработает извне!
     - Если Jenkins доступен из интернета, используйте ваш IP
   - **Content type:** `application/json`
   - **Events:** выберите "Just the push event"
   - **Active:** ✅
   - Add webhook

### Проблема с localhost:

На localhost webhook не работает, потому что GitHub не может достучаться до `localhost:8080`.

**Решения:**
- Используйте **Poll SCM** (Способ 1) - проще для зачета
- Или используйте ngrok для проброса порта (сложнее)

---

## ✅ Рекомендация для зачета

**Используйте Poll SCM (Способ 1):**

1. Настройте Poll SCM с расписанием `H/2 * * * *`
2. Сделайте изменения в коде
3. Закоммитьте и запушьте
4. Подождите до 2 минут
5. Jenkins автоматически запустит сборку

**Это проще и работает всегда!**

---

## 🧪 Тестирование

### Тест 1: Проверка Poll SCM

1. Настройте Poll SCM с расписанием `H/2 * * * *`
2. Сделайте небольшое изменение:
   ```powershell
   # Добавьте комментарий в любой файл
   echo "# Test change" >> README.md
   git add .
   git commit -m "Test automatic build"
   git push
   ```
3. Подождите до 2 минут
4. Проверьте Jenkins - должна появиться новая сборка

### Тест 2: Проверка в реальном времени

1. Откройте Jenkins
2. Откройте ваш Pipeline
3. Сделайте push в Git
4. Наблюдайте - через 2 минуты появится новая сборка

---

## 📋 Пошаговая инструкция для зачета

### Шаг 1: Настройка Poll SCM

```
1. Откройте Jenkins → ваш Pipeline → Configure
2. Найдите "Build Triggers"
3. ✅ Отметьте "Poll SCM"
4. Введите: H/2 * * * *
5. Save
```

### Шаг 2: Демонстрация

```
1. Покажите настройки Pipeline
2. Покажите, что Poll SCM включен
3. Сделайте изменения в коде
4. git add . && git commit -m "Test" && git push
5. Подождите 2 минуты
6. Покажите, что Jenkins автоматически запустил сборку
```

---

## 🆘 Если не работает

### Проблема 1: Сборка не запускается

**Проверьте:**
- Poll SCM включен? (✅ галочка стоит?)
- Расписание правильное? (`H/2 * * * *`)
- Jenkins видит репозиторий? (проверьте в настройках Pipeline)

**Решение:**
- Убедитесь, что в настройках Pipeline указан правильный URL репозитория
- Убедитесь, что указана правильная ветка (`*/master` или `*/main`)

### Проблема 2: Сборка запускается слишком часто

**Решение:**
- Измените расписание на `H/5 * * * *` (каждые 5 минут)

### Проблема 3: Jenkins не видит изменения

**Решение:**
- Проверьте, что в настройках Pipeline указан правильный URL репозитория
- Проверьте, что указана правильная ветка
- Попробуйте запустить сборку вручную (Build Now) - если работает, значит проблема в триггере

---

## 💡 Что говорить на зачете

**"Я настроил автоматический запуск сборки через Poll SCM. Jenkins каждые 2 минуты проверяет репозиторий на изменения. При обнаружении изменений автоматически запускается сборка. Это обеспечивает непрерывную интеграцию (CI) - код автоматически проверяется и собирается при каждом push в Git."**

---

## ✅ Итог

**Для зачета достаточно:**
1. ✅ Настроить Poll SCM
2. ✅ Показать, что сборка запускается автоматически
3. ✅ Объяснить, как это работает

**Это и есть автоматизация CI/CD!**

