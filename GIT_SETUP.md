# Инструкция по настройке Git репозитория

## ✅ Что нужно коммитить в Git

**ДА, коммитим:**

### 1. Конфигурационные файлы (корень проекта)
- ✅ `docker-compose.yml` - **ОБЯЗАТЕЛЬНО!** (Jenkins использует его для сборки)
- ✅ `Jenkinsfile` - **ОБЯЗАТЕЛЬНО!** (определяет pipeline CI/CD)
- ✅ `.gitignore` - правила игнорирования файлов

### 2. Конфигурации мониторинга
- ✅ `prometheus/prometheus.yml` - конфигурация Prometheus
- ✅ `grafana/provisioning/` - все файлы конфигурации Grafana
  - `datasources/prometheus.yml`
  - `dashboards/dashboard.yml`
  - `dashboards/application-metrics.json`

### 3. Документация
- ✅ `DEPLOYMENT.md` - инструкция по развертыванию
- ✅ `README_METRICS.md` - инструкция по метрикам
- ✅ `GIT_SETUP.md` - эта инструкция

### 4. Исходный код всех проектов
- ✅ `API-Gateway/` - весь проект (кроме bin/, obj/, .idea/)
- ✅ `CabinetBooking/` - весь проект (кроме bin/, obj/, .idea/)
- ✅ `User/` - весь проект (кроме bin/, obj/, .idea/)

**Включая:**
- ✅ Все `.cs` файлы (исходный код)
- ✅ Все `.csproj` файлы (проекты)
- ✅ Все `.sln` файлы (solution файлы)
- ✅ Все `Dockerfile` файлы
- ✅ Все `.proto` файлы (gRPC протобуфы)
- ✅ `appsettings.json` (конфигурация приложений)
- ✅ `Program.cs` и другие файлы кода
- ✅ Миграции Entity Framework (`Migrations/`)

## ❌ Что НЕ нужно коммитить

**НЕТ, не коммитим:**

- ❌ `bin/` и `obj/` - скомпилированные файлы (уже в .gitignore)
- ❌ `.idea/` - настройки IDE (уже в .gitignore)
- ❌ `postgresql-15/` - данные базы данных (добавлено в корневой .gitignore)
- ❌ `*.user` файлы - личные настройки (уже в .gitignore)
- ❌ Скомпилированные DLL и EXE файлы
- ❌ Логи и временные файлы

## 📋 Структура репозитория

```
CabinetBooking/                    ← Корень репозитория
├── .gitignore                     ← ✅ Коммитим
├── docker-compose.yml             ← ✅ Коммитим (ВАЖНО!)
├── Jenkinsfile                    ← ✅ Коммитим (ВАЖНО!)
├── DEPLOYMENT.md                   ← ✅ Коммитим
├── README_METRICS.md               ← ✅ Коммитим
│
├── prometheus/                     ← ✅ Коммитим всю папку
│   └── prometheus.yml
│
├── grafana/                        ← ✅ Коммитим всю папку
│   └── provisioning/
│       ├── datasources/
│       └── dashboards/
│
├── API-Gateway/                    ← ✅ Коммитим весь проект
│   ├── Dockerfile
│   ├── Gateway.sln
│   ├── .gitignore
│   └── src/
│       └── Gateway.Api/
│           ├── Program.cs
│           ├── Gateway.Api.csproj
│           ├── appsettings.json
│           └── ... (все исходники)
│
├── CabinetBooking/                 ← ✅ Коммитим весь проект
│   ├── Dockerfile
│   ├── CabinetBooking.sln
│   ├── .gitignore
│   └── src/
│       └── ... (все исходники)
│
└── User/                           ← ✅ Коммитим весь проект
    ├── Dockerfile
    ├── User.sln
    ├── .gitignore
    └── src/
        └── ... (все исходники)
```

## 🚀 Команды для первого коммита

```bash
# 1. Инициализация репозитория (если еще не сделано)
git init

# 2. Добавить все файлы (Git автоматически применит .gitignore)
git add .

# 3. Проверить, что будет закоммичено (опционально)
git status

# 4. Создать первый коммит
git commit -m "Initial commit: Add all services with Prometheus metrics and monitoring"

# 5. Добавить remote репозиторий
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>

# 6. Запушить в репозиторий
git push -u origin main
# или
git push -u origin master
```

## ⚠️ Важные замечания

### 1. docker-compose.yml - ОБЯЗАТЕЛЬНО!
Jenkins использует `docker-compose.yml` для сборки и деплоя. Без него CI/CD не будет работать!

### 2. Jenkinsfile - ОБЯЗАТЕЛЬНО!
Jenkins использует `Jenkinsfile` для определения pipeline. Без него Jenkins не знает, что делать.

### 3. Конфигурации мониторинга
Все конфигурации Prometheus и Grafana нужны для работы мониторинга. Их тоже нужно коммитить.

### 4. appsettings.json
Файлы `appsettings.json` содержат connection strings. Они нужны для работы приложений, поэтому их коммитим. 
⚠️ В продакшене лучше использовать переменные окружения или секреты!

## 🔍 Проверка перед коммитом

Перед коммитом проверьте:

```bash
# Посмотреть, что будет закоммичено
git status

# Убедиться, что docker-compose.yml включен
git status | grep docker-compose

# Убедиться, что Jenkinsfile включен
git status | grep Jenkinsfile

# Убедиться, что конфигурации мониторинга включены
git status | grep prometheus
git status | grep grafana
```

## 📝 Пример .gitignore уже настроен

Корневой `.gitignore` уже создан и настроен. Он автоматически исключит:
- `bin/`, `obj/` - скомпилированные файлы
- `.idea/` - настройки IDE
- `postgresql-15/` - данные БД
- `*.user` - личные настройки

## ✅ Итог

**ДА, коммитим весь каталог CabinetBooking**, включая:
- ✅ docker-compose.yml
- ✅ Jenkinsfile
- ✅ Все конфигурации (prometheus, grafana)
- ✅ Все исходники проектов
- ✅ Всю документацию

Git автоматически применит `.gitignore` и исключит ненужные файлы (bin/, obj/, .idea/, postgresql-15/).

